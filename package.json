{
  "name": "esp-firebase-gh",
  "version": "1.0.0",
  "description": "Google Home integration using Firebase RTDB",
  "scripts": {
    "postinstall": "cd ./functions && npm i",
    "firebase": "firebase",
    "login": "firebase login",
    "logout": "firebase logout",
    "init": "firebase init",
    "deploy": "firebase deploy",
    "deploy:dbrules": "firebase deploy --only database:rules"
  },
  "author": "mkorenko",
  "license": "MIT",
  "private": true,
  "devDependencies": {
    "firebase-tools": "11.24.0"
  }
}
